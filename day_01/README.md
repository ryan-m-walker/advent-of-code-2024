# Day 1 in C

Build and run the solution with a C99 compiler:

```sh
cc -std=c99 -Wall -Wextra -pedantic day_01.c -o day_01
./day_01 < test_input.txt
```

The program also accepts an input filename as its first argument. It prints the answers for both parts.
